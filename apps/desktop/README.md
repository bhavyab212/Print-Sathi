# Print Sathi - Desktop App

This is the Windows Electron app for print shop owners.

## Development

```bash
cd apps/desktop
npm install
npm run dev
```

## Build & Release (Windows)

To build the setup installer (.exe):

```bash
npm run package
```
The installer will be generated in `apps/desktop/release/`.
